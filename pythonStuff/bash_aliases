# Copy this code into a file named /home/pi/.bash_aliases
# This will make sure python3 is used.
# All the python code for RPi is python 3.
# The '#' character is used for a comment.
alias python='python3'
#update
alias update='sudo apt update && sudo apt upgrade';
